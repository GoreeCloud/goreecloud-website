(()=>{document.documentElement.classList.add('js');const grid=document.querySelector('#articles');if(!grid||grid.querySelector('[data-implementation-pulse]'))return;const articles=[
{tag:'AI',title:'The GoreeCloud AI repository is now established',body:'GoreeCloud AI has moved from an architectural direction into a first-party source repository. The initial repository defines the application as a native front end for Ollama designed with Glaze UI and secured through Wardveil Security. The broader Workspace, knowledge, RAG, research, file, tool, and orchestration scope remains active development rather than a Stable or production claim.',meta:'Native source milestone · August 25, 2026'},
{tag:'Mesh',title:'Mesh adds scoped authorization and durable Everkeep evidence',body:'GoreeCloud Mesh now enforces fail-closed least-privilege authorization on private mutating API routes, binds runtime evidence to producer-controlled validity, and persists Everkeep recovery evidence durably across restarts. Mesh can coordinate and expose that evidence, but it still cannot manufacture a stronger privacy, security, conformance, identity, or recovery state than the authoritative producer supports.',meta:'Platform implementation update · August 25, 2026'},
{tag:'Search',title:'Native Search gains a fail-closed category contract',body:'The native GoreeCloud Search replacement now has a bounded category request and readiness contract, server-side category handling, accessible native category controls, and fail-closed readiness behavior. The current SearXNG-derived production line remains transitional continuity while the original GoreeCloud product layer advances.',meta:'Native Search update · August 25, 2026'}
];for(const [index,item] of articles.entries()){const article=document.createElement('article');article.className=`card${index===0?' featured':''}`;article.dataset.implementationPulse='true';article.innerHTML=`<p class="tag">${item.tag}</p><h2>${item.title}</h2><p>${item.body}</p><p class="meta">${item.meta}</p>`;grid.insertBefore(article,grid.children[index]||null);}})();